from __future__ import annotations

import json


def render_app(token: str) -> str:
    token_json = json.dumps(token)
    return r'''<!doctype html>
<html lang="vi">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>WP Clean Rebuild</title>
<style>
:root{--bg:#f5f6f8;--panel:#fff;--text:#1d2735;--muted:#6e7b8d;--line:#e2e7ee;--soft:#f8fafc;--brand:#2563eb;--brand-dark:#1d4ed8;--green:#15805d;--green-bg:#edf8f3;--yellow:#9a6500;--yellow-bg:#fff8e8;--red:#bd3441;--red-bg:#fff1f2;--nav:#182233;--radius:10px}
*{box-sizing:border-box}html,body{margin:0;min-height:100%;font-family:"Segoe UI",Roboto,Arial,sans-serif;background:var(--bg);color:var(--text);font-weight:400}button,input,select{font:inherit}button{cursor:pointer;touch-action:manipulation}a{color:inherit;text-decoration:none}b,strong{font-weight:600}
.layout{display:block;min-height:100vh}.dot{display:inline-block;width:7px;height:7px;border-radius:50%;background:#2eb67d;margin-right:6px}
.main{min-width:0}.topbar{height:64px;background:#fff;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;padding:0 28px;position:sticky;top:0;z-index:20}.top-title{font-size:14px;font-weight:600}.top-actions{display:flex;align-items:center;gap:8px}.btn{border:1px solid transparent;border-radius:7px;padding:9px 13px;font-weight:600;font-size:12px;display:inline-flex;align-items:center;justify-content:center;gap:6px;min-height:44px}.btn-primary{background:var(--brand);color:#fff}.btn-primary:hover{background:var(--brand-dark)}.btn-light{background:#fff;border-color:var(--line);color:#334155}.btn-light:hover{background:#f8fafc}.btn-danger{background:var(--red-bg);border-color:#f3cbd0;color:var(--red)}.btn-warning{background:var(--yellow-bg);border-color:#efd9a8;color:var(--yellow)}.btn-success{background:var(--green-bg);border-color:#cde9dc;color:var(--green)}.btn:disabled{opacity:.55;cursor:not-allowed}.content{padding:28px;max-width:1440px;margin:auto}.page-head{display:flex;align-items:flex-end;justify-content:space-between;gap:16px;margin-bottom:18px}.page-head h1{margin:0;font-size:24px;font-weight:600;letter-spacing:-.3px}.page-head p{margin:5px 0 0;color:var(--muted);font-size:12px}.summary{display:flex;gap:22px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:var(--radius);padding:14px 16px;margin-bottom:16px}.summary-item{min-width:110px}.summary-item span{display:block;color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.45px}.summary-item b{display:block;margin-top:3px;font-size:18px;font-weight:600}.summary-sep{width:1px;height:28px;background:var(--line)}
.panel{background:#fff;border:1px solid var(--line);border-radius:var(--radius);overflow:hidden}.panel-head{padding:14px 16px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;gap:12px}.panel-head h2{margin:0;font-size:14px;font-weight:600}.search{width:280px;max-width:100%;border:1px solid var(--line);border-radius:7px;padding:8px 10px;outline:0;font-size:12px}.search:focus{border-color:#9db7ed}.project-header,.project-row{display:grid;grid-template-columns:minmax(220px,1.4fr) 130px minmax(180px,1fr) 170px 70px;gap:12px;align-items:center}.project-header{padding:10px 16px;background:#fafbfc;color:#7b8797;font-size:10px;text-transform:uppercase;letter-spacing:.4px;border-bottom:1px solid var(--line)}.project-row{padding:13px 16px;border-bottom:1px solid #eef1f5;cursor:pointer;transition:background .12s}.project-row:last-child{border-bottom:0}.project-row:hover{background:#fafcff}.project-name{display:flex;align-items:center;gap:10px;min-width:0}.site-icon{width:32px;height:32px;border-radius:7px;background:#eef3fb;color:#315c9e;display:grid;place-items:center;font-size:12px;font-weight:700;flex:0 0 auto}.project-name h3{margin:0 0 2px;font-size:13px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.project-name p{margin:0;color:var(--muted);font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.status{display:inline-flex;width:max-content;border-radius:5px;padding:5px 7px;font-size:10px;font-weight:600}.status-green{background:var(--green-bg);color:var(--green)}.status-blue{background:#eef4ff;color:#2558ae}.status-yellow{background:var(--yellow-bg);color:var(--yellow)}.status-red{background:var(--red-bg);color:var(--red)}.current-step{font-size:11px;color:#4d5a6c}.progress-wrap{display:flex;align-items:center;gap:8px}.progress{height:6px;background:#edf1f5;border-radius:5px;overflow:hidden;flex:1}.progress span{display:block;height:100%;background:#4b7ee8}.progress-num{font-size:10px;color:#667386;width:30px;text-align:right}.open-btn{border:1px solid var(--line);background:#fff;border-radius:6px;padding:7px 9px;color:#475569;font-size:11px;font-weight:500}.empty{padding:46px;text-align:center;color:var(--muted);font-size:12px}
.overlay{position:fixed;inset:0;background:rgba(25,35,50,.38);z-index:50;display:none}.overlay.show{display:block}.drawer{position:fixed;right:0;top:0;bottom:0;width:min(690px,95vw);background:#fff;z-index:60;transform:translateX(101%);transition:transform .2s ease;box-shadow:-10px 0 30px rgba(30,42,60,.12);overflow:auto}.drawer.show{transform:none}.drawer-head{padding:20px 22px 16px;border-bottom:1px solid var(--line);position:sticky;top:0;background:#fff;z-index:4}.drawer-title{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.drawer-title h2{margin:0;font-size:19px;font-weight:600}.drawer-title p{margin:4px 0 0;color:var(--muted);font-size:11px}.xbtn{border:1px solid var(--line);background:#fff;width:44px;height:44px;border-radius:7px;color:#5f6c7d;font-size:18px}.drawer-body{padding:20px 22px 36px}.section{margin-bottom:22px}.section-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:9px}.section-title{font-size:11px;text-transform:uppercase;letter-spacing:.45px;color:#7c8999;font-weight:600}.connection{border:1px solid var(--line);border-radius:9px;padding:13px;background:#fff}.connection-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px 14px}.kv span{display:block;color:var(--muted);font-size:10px;margin-bottom:3px}.kv b{display:block;font-size:11px;font-weight:500;word-break:break-word}.connection-actions{display:flex;gap:7px;margin-top:12px;padding-top:11px;border-top:1px solid #edf0f4}.steps{display:grid;border:1px solid var(--line);border-radius:9px;overflow:hidden}.step{display:grid;grid-template-columns:24px 1fr auto;align-items:center;gap:8px;padding:9px 11px;border-bottom:1px solid #edf0f4}.step:last-child{border-bottom:0}.stepdot{width:20px;height:20px;border-radius:50%;background:#eef1f5;color:#7c8999;display:grid;place-items:center;font-size:10px;font-weight:600}.step.done .stepdot{background:var(--green-bg);color:var(--green)}.step.active{background:#f7faff}.step.active .stepdot{background:#e8f0ff;color:#285bb5}.step label{font-size:11px;font-weight:500}.step small{font-size:10px;color:var(--muted)}.action-card{border:1px solid #dbe4f1;background:#f9fbfe;border-radius:9px;padding:13px;margin-top:12px}.action-card.warn{border-color:#ead8af;background:#fffaf0}.action-card.danger{border-color:#edc9cf;background:#fff7f8}.action-card h3{margin:0 0 5px;font-size:13px;font-weight:600}.action-card p{margin:0 0 10px;color:#607084;font-size:11px;line-height:1.55}.actions{display:flex;gap:7px;flex-wrap:wrap}.confirm-input{width:100%;border:1px solid #ddaab3;border-radius:7px;padding:9px 10px;margin:2px 0 10px;outline:0;font-size:12px}.checks{display:grid;gap:6px;margin:9px 0 11px}.check{display:flex;gap:8px;align-items:flex-start;border:1px solid var(--line);background:#fff;padding:9px;border-radius:7px}.check input{margin-top:2px}.check b{display:block;font-size:11px;font-weight:600}.check span{font-size:10px;color:var(--muted)}.job{border:1px solid var(--line);border-radius:9px;padding:13px;margin-top:12px;background:#fbfcfe}.jobtop{display:flex;justify-content:space-between;gap:10px}.job h3{margin:0;font-size:12px;font-weight:600}.job p{margin:4px 0;color:#677587;font-size:10px}.jobbar{height:6px;background:#e8edf3;border-radius:5px;overflow:hidden;margin:10px 0}.jobbar span{display:block;height:100%;background:#4b7ee8}.jobcur{font-size:10px;color:#7b8797;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.errorbox{margin-top:10px;border:1px solid #efc9cf;background:#fff4f5;color:#a82e3b;border-radius:7px;padding:9px;font-size:10px;white-space:pre-wrap;line-height:1.5}.logs{margin-top:9px;padding-top:8px;border-top:1px solid var(--line);max-height:110px;overflow:auto}.logs div{font:10px/1.5 Consolas,monospace;color:#7a8797}.foot-danger{border-top:1px solid var(--line);padding-top:16px;margin-top:26px;display:flex;justify-content:space-between;align-items:center;gap:12px}.foot-danger p{font-size:10px;color:var(--muted);margin:0}
.modal{position:fixed;inset:0;z-index:80;display:none;align-items:center;justify-content:center;padding:18px}.modal.show{display:flex}.modalbox{width:min(620px,96vw);max-height:92vh;overflow:auto;background:#fff;border:1px solid var(--line);border-radius:11px;box-shadow:0 20px 55px rgba(27,39,56,.18);padding:20px}.modalhead{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:16px}.modalhead h2{margin:0;font-size:18px;font-weight:600}.modalhead p{margin:4px 0 0;color:var(--muted);font-size:11px;line-height:1.45}.formgrid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field{display:grid;gap:5px}.field.full{grid-column:1/-1}.field label{font-size:10px;font-weight:600;color:#526174}.field input,.field select{width:100%;border:1px solid var(--line);border-radius:7px;padding:9px 10px;outline:0;background:#fff;font-size:12px;font-weight:400}.field input:focus,.field select:focus{border-color:#9db7ed}.hint{font-size:9px;color:#8a96a6;line-height:1.4}.modalactions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}.toast{position:fixed;right:20px;bottom:20px;background:#253044;color:#fff;border-radius:7px;padding:10px 13px;z-index:120;font-size:11px;transform:translateY(90px);opacity:0;transition:.18s}.toast.show{transform:none;opacity:1}.toast.err{background:#9d2f3a}
@media(max-width:1050px){.project-header,.project-row{grid-template-columns:minmax(210px,1.4fr) 120px minmax(160px,1fr) 140px 60px}}@media(max-width:820px){.layout{grid-template-columns:1fr}.sidebar{display:none}.topbar{padding:0 16px}.content{padding:18px 14px}.project-header{display:none}.project-row{grid-template-columns:1fr auto;gap:10px}.project-row>.current-step,.project-row>.progress-wrap{grid-column:1/-1}.project-row>.open-btn{display:none}.summary{overflow:auto}.formgrid,.connection-grid{grid-template-columns:1fr}.field.full{grid-column:auto}}
</style>
</head>
<body>
<div class="layout">
<main class="main">
  <header class="topbar"><div class="top-title">WP Clean Rebuild</div><div class="top-actions"><span id="systemHealth" class="system-health" role="status" aria-live="polite"><span class="system-health-dot"></span><span>GUI đang kết nối</span></span><button class="btn btn-light" onclick="refreshAll()">Làm mới</button><button class="btn btn-primary" onclick="openCreate()">Tạo dự án</button></div></header>
  <div class="content">
    <div class="page-head"><div><h1>Dự án WordPress</h1><p>Chọn website và tiếp tục đúng bước đang dở.</p></div></div>
    <div id="stats" class="summary"></div>
    <div class="panel"><div class="panel-head"><h2>Danh sách dự án</h2><input id="search" class="search" aria-label="Tìm dự án" placeholder="Tìm tên dự án hoặc domain" oninput="renderProjects()"></div>
      <div class="project-header"><div>Dự án</div><div>Trạng thái</div><div>Bước hiện tại</div><div>Tiến độ</div><div></div></div>
      <div id="projects"></div>
    </div>
  </div>
</main>
</div>
<div id="overlay" class="overlay" onclick="closePanels()"></div>
<aside id="drawer" class="drawer" aria-label="Chi tiết dự án"><div id="drawerContent"></div></aside>

<div id="createModal" class="modal" role="dialog" aria-modal="true" aria-labelledby="createTitle"><div class="modalbox"><div class="modalhead"><div><h2 id="createTitle">Tạo dự án mới</h2></div><button class="xbtn" type="button" aria-label="Đóng cửa sổ tạo dự án" onclick="closeCreate()">×</button></div>
<form id="createForm" onsubmit="submitCreate(event)"><div class="formgrid">
<div class="field"><label>Tên dự án</label><input id="f_name" name="name" placeholder="noi-that-dai-duong"></div>
<div class="field"><label>FTP host *</label><input id="f_host" name="host" required placeholder="example.com" oninput="syncDefaults()"></div>
<div class="field"><label>Tài khoản FTP *</label><input name="username" required autocomplete="username"></div>
<div class="field"><label>Mật khẩu FTP *</label><input name="password" type="password" required autocomplete="current-password"></div>
<div class="field"><label>Giao thức</label><select name="protocol"><option value="ftp">FTP</option><option value="ftps">FTPS</option></select></div>
<div class="field"><label>Port</label><input name="port" type="number" value="21"></div>
<div class="field full"><label>Remote WordPress path</label><input id="f_remote" name="remotePath" placeholder="/domains/example.com/public_html"></div>
<div class="field full"><label>URL website</label><input id="f_url" name="siteUrl" placeholder="https://example.com"></div>
<div class="field"><label>Workers</label><input name="workers" type="number" min="1" max="16" value="4"></div>
<div class="field"><label>Block MiB</label><input name="blockMb" type="number" min="1" max="8" value="1"></div>
</div><div class="modalactions"><button type="button" class="btn btn-light" onclick="closeCreate()">Hủy</button><button class="btn btn-primary" type="submit">Tạo dự án</button></div></form></div></div>

<div id="editModal" class="modal" role="dialog" aria-modal="true" aria-labelledby="editTitle"><div class="modalbox"><div class="modalhead"><div><h2 id="editTitle">Sửa kết nối FTP</h2></div><button class="xbtn" type="button" aria-label="Đóng cửa sổ sửa FTP" onclick="closeEdit()">×</button></div>
<form id="editForm" onsubmit="submitEdit(event)"><input type="hidden" id="e_project" name="_updateProject"><div class="formgrid">
<div class="field"><label for="e_host">FTP host *</label><input id="e_host" name="host" required spellcheck="false"><div class="hint">Nếu project đã bắt đầu, hệ thống sẽ không cho đổi sang host khác.</div></div>
<div class="field"><label for="e_username">Tài khoản FTP *</label><input id="e_username" name="username" required autocomplete="username" spellcheck="false"></div>
<div class="field"><label for="e_password">Mật khẩu FTP</label><input id="e_password" name="password" type="password" autocomplete="new-password" placeholder="Để trống nếu không đổi"></div>
<div class="field"><label for="e_protocol">Giao thức</label><select id="e_protocol" name="protocol"><option value="ftp">FTP</option><option value="ftps">FTPS</option></select></div>
<div class="field third"><label for="e_port">Port</label><input id="e_port" name="port" type="number"></div>
<div class="field third"><label for="e_workers">Workers</label><input id="e_workers" name="workers" type="number" min="1" max="16"></div>
<div class="field third"><label for="e_block">Block MiB</label><input id="e_block" name="blockMb" type="number" min="1" max="8"></div>
<div class="field full"><label for="e_remote">Remote WordPress path</label><input id="e_remote" name="remotePath" spellcheck="false"></div>
<div class="field full"><label for="e_url">URL website</label><input id="e_url" name="siteUrl" type="url" spellcheck="false"></div>
</div><div class="modalactions"><button type="button" class="btn btn-light" onclick="closeEdit()">Hủy</button><button type="button" class="btn btn-light" onclick="saveEdit(false)">Chỉ lưu</button><button class="btn btn-primary" type="submit">Lưu & kiểm tra kết nối</button></div></form></div></div>
<div id="toast" class="toast" role="status" aria-live="polite"></div>

<script>
const TOKEN=''' + token_json + r''';
let state={projects:[],selected:null};
const qs=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function formatDuration(value){const total=Math.max(0,Number(value||0)),h=Math.floor(total/3600),m=Math.floor((total%3600)/60),s=Math.floor(total%60);return h?`${h}h ${String(m).padStart(2,'0')}m`:m?`${m}m ${String(s).padStart(2,'0')}s`:`${s}s`}
function formatSignal(value){const seconds=Math.max(0,Number(value||0));return seconds<5?'vừa cập nhật':`cập nhật ${formatDuration(seconds)} trước`}
function formatBytes(value){let n=Math.max(0,Number(value||0));if(n<1024)return `${Math.round(n)} B`;const units=['KB','MB','GB','TB'];let i=-1;do{n/=1024;i++}while(n>=1024&&i<units.length-1);return `${n>=100?n.toFixed(0):n>=10?n.toFixed(1):n.toFixed(2)} ${units[i]}`}
function setSystemHealth(online,message=''){const el=qs('#systemHealth');if(!el)return;el.classList.toggle('offline',!online);const label=el.querySelector('span:last-child');if(label)label.textContent=online?(message||'GUI đang hoạt động'):(message||'Mất kết nối với GUI')}
async function api(url,opt={}){const timeoutMs=Number(opt.timeoutMs||(String(opt.method||'GET').toUpperCase()==='POST'?120000:15000)),controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);const o={...opt,signal:controller.signal,headers:{'Content-Type':'application/json','X-WPClean-Token':TOKEN,...(opt.headers||{})}};delete o.timeoutMs;let r;try{r=await fetch(url,o);setSystemHealth(true)}catch(err){const timeout=err?.name==='AbortError',e=new Error(timeout?'[GUI-API-TIMEOUT-001] GUI chờ phản hồi quá lâu. Kiểm tra cửa sổ chạy tool rồi thử lại.':'[GUI-OFFLINE-001] Không kết nối được với ứng dụng local.');e.code=timeout?'GUI-API-TIMEOUT-001':'GUI-OFFLINE-001';setSystemHealth(false,timeout?'GUI phản hồi chậm':'GUI mất kết nối');throw e}finally{clearTimeout(timer)}let d={};try{d=await r.json()}catch{}if(!r.ok){const prefix=d.errorCode?`[${d.errorCode}] `:'',title=d.errorTitle?`${d.errorTitle}: `:'',e=new Error(prefix+title+(d.error||('HTTP '+r.status)));e.code=d.errorCode||'';e.title=d.errorTitle||'';e.recovery=d.recovery||'';e.retryable=d.retryable!==false;e.technical=d.technical||'';throw e}return d}
function toast(msg,err=false){const el=qs('#toast');el.textContent=msg;el.className='toast show'+(err?' err':'');clearTimeout(window._tt);window._tt=setTimeout(()=>el.className='toast',3200)}
function beginBusy(label='Đang xử lý...'){const button=document.activeElement;if(!(button instanceof HTMLButtonElement))return()=>{};const previous=button.innerHTML;button.disabled=true;button.setAttribute('aria-busy','true');button.textContent=label;return()=>{if(button.isConnected){button.disabled=false;button.removeAttribute('aria-busy');button.innerHTML=previous}}}
function progressOf(p){if(!p.steps?.length)return 0;const done=p.steps.filter(x=>x.done).length,running=p.job?.status==='running'?Math.max(0,Math.min(100,Number(p.job.percent||0)))/100:0;return Math.round((done+running)/p.steps.length*100)}
function authError(text){return /(^|\s)530(\s|:)|login authentication failed|authentication failed|not logged in/i.test(String(text||''))}
function statusInfo(p){const j=p.job;if(j?.status==='running'&&j?.health==='stalled')return ['status-red','Có dấu hiệu treo'];if(j?.status==='running'&&['waiting','slow'].includes(j?.health))return ['status-yellow','Đang chờ hosting'];if(j?.status==='running')return ['status-blue','Đang chạy'];if(j?.status==='error')return ['status-red',j.errorCode||'Có lỗi'];if(j?.status==='needs-action')return ['status-yellow','Cần xác nhận'];if(j?.status==='paused')return ['status-yellow','Tạm dừng'];if(p.completed)return ['status-green','Hoàn tất'];return ['status-blue','Đang xử lý']}
function renderStats(){const a=state.projects,total=a.length,done=a.filter(x=>x.completed).length,running=a.filter(x=>x.job?.status==='running').length,attention=a.filter(x=>['error','needs-action','paused'].includes(x.job?.status)).length;qs('#stats').innerHTML=`<div class="summary-item"><span>Tổng dự án</span><b>${total}</b></div><div class="summary-sep"></div><div class="summary-item"><span>Hoàn tất</span><b>${done}</b></div><div class="summary-sep"></div><div class="summary-item"><span>Đang chạy</span><b>${running}</b></div><div class="summary-sep"></div><div class="summary-item"><span>Cần xử lý</span><b>${attention}</b></div>`}
function renderProjects(){const q=(qs('#search')?.value||'').toLowerCase(),list=state.projects.filter(p=>(p.name+' '+p.host).toLowerCase().includes(q));qs('#projects').innerHTML=list.length?list.map(p=>{const pc=progressOf(p),si=statusInfo(p),encoded=encodeURIComponent(p.name);return `<div class="project-row" role="button" tabindex="0" aria-label="Mở dự án ${esc(p.name)}" onclick="openProject('${encoded}')" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();openProject('${encoded}')}"><div class="project-name"><div class="site-icon">WP</div><div><h3>${esc(p.name)}</h3><p>${esc(p.host)}</p></div></div><div><span class="status ${si[0]}">${si[1]}</span></div><div class="current-step">${esc(p.nextLabel||'')}</div><div class="progress-wrap"><div class="progress"><span style="width:${pc}%"></span></div><div class="progress-num">${pc}%</div></div><span class="open-btn" aria-hidden="true">Mở</span></div>`}).join(''):'<div class="empty">Chưa có dự án.<br><br><button class="btn btn-primary" onclick="openCreate()">Tạo dự án đầu tiên</button></div>'}
async function refreshProject(name){const p=await api('/api/projects/'+encodeURIComponent(name));if(state.selected===name)renderDrawer(p);return p}
async function refreshAll(){try{const d=await api('/api/projects');state.projects=d.projects||[];renderStats();renderProjects();if(state.selected)await refreshProject(state.selected);setSystemHealth(true,'Đã đồng bộ '+new Date().toLocaleTimeString('vi-VN',{hour:'2-digit',minute:'2-digit',second:'2-digit'}))}catch(e){toast(e.message,true)}}
function stepHtml(p){const active=p.nextStage,running=p.job?.status==='running';return (p.steps||[]).map((s,i)=>{const isActive=!s.done&&s.key===active;return `<div class="step ${s.done?'done':''} ${isActive?'active':''}"><div class="stepdot">${s.done?'✓':i+1}</div><label>${esc(s.label)}</label><small>${s.done?'Hoàn tất':(isActive?(running?'Đang chạy':'Tiếp theo'):'Chưa chạy')}</small></div>`}).join('')}
function jobHtml(p){const j=p.job;if(!j||j.status==='idle')return '';const si=statusInfo(p),health=j.health||j.status||'idle',pct=Math.max(0,Math.min(100,Number(j.percent||0))),info=j.errorInfo||null,pg=j.progress||{},unit=esc(pg.unit||'file'),itemText=pg.total?`${pg.completed||0}/${pg.total} ${unit}`:pg.completed?`${pg.completed} ${unit} đã kiểm kê`:pct?`${pct}%`:'Đang xác định',speed=pg.bytesPerSecond?`${formatBytes(pg.bytesPerSecond)}/s`:'—';const metrics=`<div class="jobmeta"><div class="jobmetric"><span>Thời gian chạy</span><b>${formatDuration(j.elapsedSeconds||0)}</b></div><div class="jobmetric"><span>Tín hiệu cuối</span><b>${formatSignal(j.idleSeconds||0)}</b></div><div class="jobmetric"><span>Số lượng</span><b>${itemText}${pg.failed?` · lỗi ${pg.failed}`:''}</b></div><div class="jobmetric"><span>Tốc độ</span><b>${speed}</b></div></div>`;const error=info?`<div class="error-summary" role="alert"><span class="error-code">${esc(info.code||'WPCLEAN-UNEXPECTED-001')}</span><h4>${esc(info.title||j.title||'Bước xử lý dừng do lỗi')}</h4><p>${esc(info.message||j.error||'')}</p>${info.recovery?`<p class="error-recovery"><b>Cách xử lý:</b> ${esc(info.recovery)}</p>`:''}${info.technical?`<details class="technical-details"><summary>Chi tiết kỹ thuật</summary><pre>${esc(info.technical)}</pre></details>`:''}</div>`:(j.error?`<div class="errorbox" role="alert">${esc(j.error)}</div>`:'');return `<div class="job"><div class="jobtop"><div><h3>${esc(j.title||j.stage)}</h3><p>${esc(j.message||'')}</p></div><span class="status ${si[0]}">${si[1]}</span></div><div class="job-health ${esc(health)}"><span class="job-health-dot"></span>${esc(j.healthLabel||si[1])}</div>${metrics}${j.status==='running'?`<div class="jobbar ${pct?'':'indeterminate'}"><span style="width:${pct||34}%"></span></div><div class="jobcur">${esc(j.current||'Hệ thống vẫn đang chờ tín hiệu từ bước xử lý...')}</div>`:''}${error}${j.logs?.length?`<div class="logs">${j.logs.slice(-7).map(x=>`<div>${esc(x)}</div>`).join('')}</div>`:''}</div>`}
function decisionHtml(p){const j=p.job,d=j?.decision;if(j?.status==='running')return '';if(j?.status==='error'){const info=j.errorInfo||{},ftp=(j.errorCode==='FTP-AUTH-001')||authError(j.error);return `<div class="action-card ${ftp?'danger':''}"><h3>${ftp?'Cần sửa thông tin FTP':'Bước xử lý đã dừng an toàn'}</h3><p>${esc(info.recovery||'Xử lý nguyên nhân được nêu phía trên rồi tiếp tục đúng bước hiện tại.')}</p><div class="actions">${ftp?`<button class="btn btn-primary" onclick="openEditFtp('${esc(p.name)}',true)">Sửa thông tin FTP</button>`:''}${info.retryable===false?'':`<button class="btn btn-light" onclick="advance('${esc(p.name)}',{})">Thử lại đúng bước</button>`}</div></div>`}
if(!d){if(p.completed)return `<div class="action-card"><h3>Dự án đã hoàn tất</h3><p>Website đã được xác nhận. Có thể mở website hoặc dọn dữ liệu local khi không còn cần backup.</p><div class="actions"><a class="btn btn-success" target="_blank" href="${esc(p.url)}">Mở website</a><a class="btn btn-light" target="_blank" href="${esc(p.adminUrl)}">Mở wp-admin</a></div></div>`;return `<div class="action-card"><h3>Tiếp tục xử lý</h3><p>Bước tiếp theo: <b>${esc(p.nextLabel)}</b>. Hệ thống sẽ chạy liên tiếp cho tới điểm cần xác nhận.</p><div class="actions"><button class="btn btn-primary" onclick="advance('${esc(p.name)}',{})">Tiếp tục</button><button class="btn btn-light" onclick="testFtp('${esc(p.name)}')">Kiểm tra FTP</button></div></div>`}
if(d.type==='rebuild')return `<div class="action-card danger"><h3>Xác nhận rebuild WordPress</h3><p>${esc(j.message)}<br><b>Remote:</b> ${esc(d.remotePath)}</p><div class="actions"><button class="btn btn-danger" onclick="confirmRebuild('${esc(p.name)}','${esc(d.confirmText)}')">Xác nhận xóa code cũ & rebuild</button></div></div>`;
if(d.type==='resume-db')return `<div class="action-card warn"><h3>Tiếp tục database an toàn</h3><p>${esc(j.message)}</p><button class="btn btn-warning" onclick="advance('${esc(p.name)}',{resumeDb:true})">Tiếp tục import DB-only</button></div>`;
if(d.type==='theme')return `<div class="action-card"><h3>Theme</h3><p>${esc(j.message)}</p><div class="checks"><label class="check"><input id="themeParent" type="checkbox" checked><div><b>Cài Flatsome sạch</b><span>Dùng themes/flatsome.zip</span></div></label>${d.hasChild?`<label class="check"><input id="themeChild" type="checkbox" checked><div><b>Quét và cài theme con ${esc(d.childSlug)}</b><span>Chỉ upload khi scan PASS${d.repairPath?' · có working-copy':''}</span></div></label>`:''}</div><button class="btn btn-primary" onclick="runTheme('${esc(p.name)}',${d.hasChild?'true':'false'})">Tiếp tục theme</button></div>`;
if(d.type==='theme-repair')return `<div class="action-card warn"><h3>Theme con cần sửa</h3><p>${esc(j.message)}<br><b>${esc(d.path)}</b></p><div class="actions"><button class="btn btn-warning" onclick="openRepair('${esc(p.name)}')">Mở thư mục sửa</button><button class="btn btn-primary" onclick="advance('${esc(p.name)}',{themeInstall:true,childInstall:true})">Quét lại và tiếp tục</button></div></div>`;
if(d.type==='manual-theme')return `<div class="action-card warn"><h3>Theme cần cài thủ công</h3><p>${esc(j.message)}<br>Theme: <b>${esc(d.theme)}</b></p><button class="btn btn-primary" onclick="advance('${esc(p.name)}',{manualThemeAck:true})">Tôi đã cài theme sạch</button></div>`;
if(d.type==='manual-plugins')return `<div class="action-card warn"><h3>${d.count} plugin cần cài thủ công</h3><p>Lấy bản sạch từ nhà cung cấp. Không dùng lại plugin code trong backup website nhiễm.</p><div class="actions"><button class="btn btn-primary" onclick="advance('${esc(p.name)}',{manualPluginsAck:true,manualPluginsUploaded:true})">Tôi đã cài xong</button><button class="btn btn-light" onclick="advance('${esc(p.name)}',{manualPluginsAck:true,manualPluginsUploaded:false})">Xử lý sau</button></div></div>`;
if(d.type==='final'){const h=d.home||{},a=d.admin||{};return `<div class="action-card"><h3>Kiểm tra nhanh website</h3><p>Trang chủ: <b>${h.ok?'PASS':'Cần kiểm tra'} · HTTP ${h.status||'?'}</b><br>wp-admin: <b>${a.ok?'PASS':'Cần kiểm tra'} · HTTP ${a.status||'?'}</b><br><br>Mở website và kiểm tra warning PHP, header/footer, ảnh và chức năng chính.</p><div class="actions"><a class="btn btn-light" target="_blank" href="${esc(d.url)}">Mở frontend</a><a class="btn btn-light" target="_blank" href="${esc(d.adminUrl)}">Mở wp-admin</a><button class="btn btn-success" onclick="advance('${esc(p.name)}',{finalChoice:'complete'})">Website ổn, hoàn tất</button><button class="btn btn-warning" onclick="advance('${esc(p.name)}',{finalChoice:'problem'})">Có lỗi, chờ kỹ thuật</button><button class="btn btn-light" onclick="advance('${esc(p.name)}',{finalChoice:'deep'})">Quét sâu</button></div></div>`}return ''}
function connectionHtml(p){const c=p.connection||{};return `<div class="connection"><div class="connection-grid"><div class="kv"><span>FTP host</span><b>${esc(c.host||p.host)}</b></div><div class="kv"><span>Tài khoản</span><b>${esc(c.username||'')}</b></div><div class="kv"><span>Giao thức / Port</span><b>${esc((c.protocol||p.protocol||'').toUpperCase())} · ${esc(c.port||'')}</b></div><div class="kv"><span>Mật khẩu</span><b>${c.passwordConfigured?'Đã lưu':'Chưa có'}</b></div><div class="kv"><span>Remote WordPress</span><b>${esc(c.remotePath||p.remotePath)}</b></div><div class="kv"><span>Website</span><b>${esc(c.siteUrl||p.url)}</b></div></div><div class="connection-actions"><button class="btn btn-light" onclick="testFtp('${esc(p.name)}')">Kiểm tra kết nối</button><button class="btn btn-primary" onclick="openEditFtp('${esc(p.name)}')">Sửa FTP</button></div></div>`}
function renderDrawer(p){state.selected=p.name;const pc=progressOf(p);qs('#drawerContent').innerHTML=`<div class="drawer-head"><div class="drawer-title"><div><h2>${esc(p.host)}</h2><p>${esc(p.name)} · ${pc}% hoàn tất</p></div><button class="xbtn" type="button" aria-label="Đóng chi tiết dự án" onclick="closePanels()">×</button></div></div><div class="drawer-body"><div class="section"><div class="section-head"><div class="section-title">Kết nối FTP</div></div>${connectionHtml(p)}</div><div class="section"><div class="section-head"><div class="section-title">Tiến độ xử lý</div></div><div class="steps">${stepHtml(p)}</div>${jobHtml(p)}${decisionHtml(p)}</div>${p.themeRepair?`<div class="section"><button class="btn btn-warning" onclick="openRepair('${esc(p.name)}')">Mở thư mục theme repair</button></div>`:''}${p.completed?`<div class="foot-danger"><button class="btn btn-danger" onclick="deleteProject('${esc(p.name)}')">Xóa dự án local</button></div>`:''}</div>`}
async function openProject(encoded){const name=decodeURIComponent(encoded);try{const p=await api('/api/projects/'+encodeURIComponent(name));state.selected=name;renderDrawer(p);qs('#overlay').classList.add('show');qs('#drawer').classList.add('show')}catch(e){toast(e.message,true)}}
function closePanels(){state.selected=null;qs('#overlay').classList.remove('show');qs('#drawer').classList.remove('show');closeCreate();closeEdit()}
function openCreate(){qs('#overlay').classList.add('show');qs('#createModal').classList.add('show')}
function closeCreate(){qs('#createModal').classList.remove('show');if(!qs('#drawer').classList.contains('show')&&!qs('#editModal').classList.contains('show'))qs('#overlay').classList.remove('show')}
function syncDefaults(){const h=qs('#f_host').value.trim();if(!qs('#f_name').value)qs('#f_name').value=h.replace(/[^a-zA-Z0-9._-]+/g,'-').toLowerCase();qs('#f_remote').value=h?`/domains/${h}/public_html`:'';qs('#f_url').value=h?`https://${h}`:''}
async function submitCreate(e){e.preventDefault();const done=beginBusy('Đang tạo...'),fd=new FormData(e.target),d=Object.fromEntries(fd.entries());d.port=Number(d.port||21);d.workers=Number(d.workers||4);d.blockMb=Number(d.blockMb||1);d.passive=true;try{const p=await api('/api/projects',{method:'POST',body:JSON.stringify(d)});closeCreate();e.target.reset();await refreshAll();toast('Đã tạo dự án '+p.name);openProject(encodeURIComponent(p.name))}catch(err){toast(err.message,true)}finally{done()}}
async function openEditFtp(name,focusPassword=false){try{const p=await api('/api/projects/'+encodeURIComponent(name)),c=p.connection||{};qs('#e_project').value=p.name;qs('#e_host').value=c.host||p.host||'';qs('#e_username').value=c.username||'';qs('#e_password').value='';qs('#e_protocol').value=(c.protocol||'ftp').toLowerCase().startsWith('ftps')?'ftps':'ftp';qs('#e_port').value=c.port||21;qs('#e_workers').value=c.workers||p.workers||4;qs('#e_remote').value=c.remotePath||p.remotePath||'';qs('#e_url').value=c.siteUrl||p.url||'';qs('#e_block').value=c.blockMb||1;qs('#overlay').classList.add('show');qs('#editModal').classList.add('show');setTimeout(()=>focusPassword?qs('#e_password').focus():qs('#e_username').focus(),50)}catch(e){toast(e.message,true)}}
function closeEdit(){qs('#editModal').classList.remove('show');if(!qs('#drawer').classList.contains('show')&&!qs('#createModal').classList.contains('show'))qs('#overlay').classList.remove('show')}
function editPayload(){const fd=new FormData(qs('#editForm')),d=Object.fromEntries(fd.entries());d.port=Number(d.port||21);d.workers=Number(d.workers||4);d.blockMb=Number(d.blockMb||1);d.passive=true;return d}
async function saveEdit(doTest=true){const done=beginBusy(doTest?'Đang lưu & kiểm tra...':'Đang lưu...'),d=editPayload(),name=d._updateProject;if(!name){done();return}try{await api('/api/projects',{method:'POST',body:JSON.stringify(d)});await refreshAll();if(doTest){toast('Đã lưu. Đang kiểm tra FTP...');await api('/api/projects/'+encodeURIComponent(name)+'/test',{method:'POST',body:'{}'});toast('Kết nối FTP thành công');closeEdit()}else{toast('Đã lưu thông tin FTP');closeEdit()}if(state.selected===name)await refreshProject(name)}catch(e){toast(e.message,true);if(authError(e.message)||e.code==='FTP-AUTH-001')qs('#e_password').focus()}finally{done()}}
function submitEdit(e){e.preventDefault();saveEdit(true)}
async function testFtp(name){const done=beginBusy('Đang kiểm tra...');try{toast('Đang kiểm tra FTP...');const d=await api('/api/projects/'+encodeURIComponent(name)+'/test',{method:'POST',body:'{}'});toast('Kết nối FTP thành công · '+d.remotePath)}catch(e){toast(e.message,true);if(authError(e.message)||e.code==='FTP-AUTH-001')openEditFtp(name,true)}finally{done()}}
async function advance(name,opts){const done=beginBusy('Đang bắt đầu...');try{await api('/api/projects/'+encodeURIComponent(name)+'/advance',{method:'POST',body:JSON.stringify(opts||{})});toast('Đã bắt đầu xử lý');startPolling(name)}catch(e){toast(e.message,true)}finally{done()}}
function confirmRebuild(name,expected){advance(name,{confirmRebuild:expected})}
function runTheme(name,hasChild){advance(name,{themeInstall:!!qs('#themeParent')?.checked,childInstall:hasChild?!!qs('#themeChild')?.checked:false})}
async function openRepair(name){try{const d=await api('/api/projects/'+encodeURIComponent(name)+'/open-repair',{method:'POST',body:'{}'});toast('Đã mở '+d.path)}catch(e){toast(e.message,true)}}
async function deleteProject(name){if(!confirm(`Xóa toàn bộ dữ liệu local của dự án ${name}?`))return;try{await api('/api/projects/'+encodeURIComponent(name)+'/delete',{method:'POST',body:JSON.stringify({confirmation:name})});closePanels();await refreshAll();toast('Đã xóa dự án local '+name)}catch(e){toast(e.message,true)}}
let pollTimer=null,pollErrorShown=false;function startPolling(name){clearInterval(pollTimer);pollTimer=setInterval(async()=>{try{const d=await api('/api/projects');state.projects=d.projects||[];renderStats();renderProjects();const p=await refreshProject(name);pollErrorShown=false;if(p.job&&p.job.status!=='running'){clearInterval(pollTimer);pollTimer=null}}catch(e){if(!pollErrorShown){toast(e.message,true);pollErrorShown=true}}},1000)}
setInterval(()=>{if(!pollTimer)refreshAll()},6000);refreshAll();
</script>
</body></html>'''


__all__ = ["render_app"]
